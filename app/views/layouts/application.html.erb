<!DOCTYPE html>
<html>
<head>
  <title>Gdhost</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
<div class="container">
  <div class="header_text">
    <% if session[:user_id] == nil %>
    <a class="header_link" href="/login_page">Login</a> |
    <a class="header_link" href="/signup">Signup</a>
    <%else%>
    <label >Hello <%=User.find_by(id: session[:user_id]).username%></label>
    <a class="header_link" href="/logout">Logout</a>
    <%end%>
  </div>
  <br>
  <div class="home_text">
    <a href="/">Home</a>
  </div>
  <br>
  <div class="yield_content">
    <%= yield %>
  </div>
</div>
</body>
</html>

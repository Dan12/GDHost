<div class="editu-container section-container">
  <div class="section-header">
    <h2 class="editu-header">Edit Profile</h2>
  </div>
  <div class="section-content">
    <%if session[:error_msg] != nil%>
    <div class="edit_error"><%=session[:error_msg]%></div>
    <%end%>
    <form action="/users/update/<%=@user.id%>">
      <div class="form-input">
        <label>Username</label>
        <input type="text" name="username" value="<%=@user.username%>">
      </div>
      <div class="form-input">
        <label>Password</label>
        <input type="password" name="password">
      </div>
      <div class="form-input">
        <label>Confirm Password</label>
        <input type="password" name="password_confirmation">
      </div>
      <input type="submit" class="button-1" value="Save">
    </form>
  </div>
</div>